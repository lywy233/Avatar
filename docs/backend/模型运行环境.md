# 模型运行环境

模型运行环境关联

1. 工作空间
工作空间是一个基于文件系统的模型运行目录，其最重要的作用是
文件的读写，查找请求

2. 命令行环境
命令行环境 本质是一个命令行执行工具，一般支持异步和同步的

3. 工具组
工具组定义了模型能够直接使用的工具,这是模型能直接调用的，本质是json schema 和执行函数的合集 

4. 运行时状态
运行时状态，运行时状态时一个基于运行时配置fork出来的运行状态，这个状态伴随这智能体执行进行变化
变化包括
运行中的任务，运行中不断更改的配置等（例如剩余迭代次数，token临时计数等）